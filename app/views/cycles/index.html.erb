<h4 style="margin-bottom:0;">
  <%= link_to @current_studio.name, @current_studio.path || '/' %> / Cycles
</h4>
<h1 style="margin-top:0;">Cycles</h1>

<h2>Current</h2>
<%= render 'shared/cycle_count_table', cycles: @current_cycles %>

<h2>Recent</h2>
<%= render 'shared/cycle_count_table', cycles: @recent_cycles %>

<h2>Future</h2>
<%= render 'shared/cycle_count_table', cycles: @future_cycles %>