<% if current_user %>
  <h1>Teams</h1>
  <ul>
    <% @teams.each do |team| %>
      <li><a href="<%= team.path %>"><%= team.name %></a></li>
    <% end %>
  </ul>
  <!-- <p><a href="<%= new_team_path %>">Start a new team</a></p>
  <p><a href="">Join an existing team</a></p> -->
<% end %>