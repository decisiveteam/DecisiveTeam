<h4 style="margin-bottom:0;">
  <%= link_to @current_studio.name, @current_studio.path || '/' %>
</h4>
<h1 style="margin-top:0;">Settings</h1>
<%= form_with(url: "#{@current_studio.path}/settings") do |form| %>
  <h2>Studio Name</h2>
  <p>
    <%= form.text_field :name, placeholder: 'Studio Name', value: @current_studio.name %>
  </p>

  <h2>Time Zone</h2>
  <p>
    The studio time zone determines when cycles begin and end.
  </p>
  <p>
    <%= form.time_zone_select :timezone, ActiveSupport::TimeZone.all, default: @current_studio.timezone.name %>
  </p>

  <!-- <h2>Experimental Features</h2>
  <p>
    <label>
      <%= form.check_box :pages_enabled, checked: @current_studio.pages_enabled? %>
      Enable <%= octicon 'note', style: 'margin-bottom:1px;' %> Pages (markdown webpages only visible to studio members)
    </label>
  </p>
  <p>
    <label>
      <%= form.check_box :random_enabled, checked: @current_studio.random_enabled? %>
      Enable <%= octicon 'broadcast', style: 'margin-bottom:1px;' %> Decision Beacons (randomly selected values announced at regular intervals)
    </label>
  </p> -->

  <hr style="height:1px;" />
  <%= form.submit 'Save' %>
<% end %>
